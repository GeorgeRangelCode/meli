import Breadcrumb from "./Breadcrumb";
import "../styles/components/ListItems.scss";

const ListItems = () => {
  return (
    <div className="list-items">
      <Breadcrumb />
    </div>
  );
};

export default ListItems;
